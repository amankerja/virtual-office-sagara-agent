# Sagara Mission Control Frontend Environment
# Local development API endpoint (Do NOT use production or VPS addresses)
VITE_MISSION_CONTROL_API_URL=http://localhost:8000

# Prototype Data Mode: 'mock' (default prototype mode) | 'api' (requires live Mission Control backend)
VITE_DATA_MODE=mock
